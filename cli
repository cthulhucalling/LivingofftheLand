Look for "password" in files (recusrive, case insensitive)
findstr /si "password" *.txt (look for *.txt, *.xml, *.ini and others)

Attempt a zone transfer using nslookup:
 nslookup
 set type=any
 ls -d <domain name>
